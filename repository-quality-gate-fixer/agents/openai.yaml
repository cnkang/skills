interface:
  display_name: "Repository Quality Gate Fixer"
  short_description: "Audit or repair quality gates with current evidence"
  default_prompt: "Use $repository-quality-gate-fixer to diagnose and fix the current quality-gate failures, verify the result, and report remaining blockers without committing or pushing."

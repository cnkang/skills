interface:
  display_name: "Conventional Commit Batcher"
  short_description: "Split mixed changes into logical Conventional Commits"
  default_prompt: "Use $conventional-commit-batcher to split my changes into logical commits, validate each batch, and commit the requested scope."
